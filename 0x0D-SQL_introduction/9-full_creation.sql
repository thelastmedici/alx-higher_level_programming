-- create table second_table in the database hbtn_0c_0
-- The table second_table should have the same columns as first_table
-- The id column should be a primary key

CREATE IF NOT EXISTS hbtn_0c_0.second_table (id INT, name VARCHAR(256), score INT);